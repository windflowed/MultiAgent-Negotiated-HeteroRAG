"""
Prompt 模板集中管理
所有 Agent 使用的 Prompt 模板统一定义在此模块，通过变量引用
"""

# ===== 查询路由 Agent =====
# 零样本分类意图识别，输出需要激活的数据源列表
ROUTER_PROMPT = """你是一个查询路由专家。根据用户问题，判断需要查询哪些数据源来回答。

数据源类型：
- sql: 结构化数据库，包含电影的数值属性（评分、票房、上映时间等）
- doc: 非结构化文档，包含电影的详细描述、剧情介绍、背景信息
- kg: 知识图谱，包含电影-演员-导演-类型的关系网络

请分析用户问题，返回需要激活的数据源列表（JSON格式）。

输出格式：
{{
    "sources": ["数据源1", "数据源2"],
    "reason": "选择原因简述"
}}

注意：
1. 如果问题涉及数值统计、排名、筛选，需要 sql
2. 如果问题需要详细描述、背景信息，需要 doc
3. 如果问题涉及人物关系、合作网络，需要 kg
4. 多源组合规则（重要）：
   - 问题同时涉及人物关系（演员/导演）和数值条件（评分/票房/时间）时，必须同时激活 kg + sql
   - 例如："某演员主演了哪些评分8.0以上的电影" → 需要 kg（查演员→电影关系）+ sql（查评分并过滤）
   - 例如："某导演执导的电影中票房最高的是哪部" → 需要 kg + sql
   - 例如："某类型中评分最高的电影的导演是谁" → 需要 sql + kg
5. 如果无法确定，默认激活全部三个数据源

用户问题：{query}"""


# ===== SQL 查询 Agent =====
# 自然语言转 SQL，搭配 CoT 思维链
SQL_COT_PROMPT = """你是一个 SQL 专家。根据用户问题，生成正确的 SQLite 查询语句。

数据库表结构：
{table_schema}

请按以下步骤思考：
1. 理解用户问题的核心需求
2. 确定需要查询的表和字段
3. 数据质量检查（重要）：
   - 查询评分最高的电影时，应排除冷门电影，只考虑票房超过100万美元的电影（revenue > 1000000）
   - 查询演员/导演相关的电影数量时，同样排除票房低于100万美元的电影
   - 优先返回有足够数据支撑的结果
   - 确保结果是用户真正关心的知名电影，而非评分虚高的冷门电影
4. 中文名优先：
   - 查询电影信息时，优先返回 title_zh（中文名）字段
   - 如果 title_zh 为空或无中文名，则返回 title（英文名）
   - 在 SELECT 中同时包含 title_zh 和 title，以 title_zh 为主要展示字段
5. 构建 SQL 查询语句
6. 验证 SQL 语法是否正确

用户问题：{query}

请输出：
1. 思考过程（Chain of Thought）
2. 最终的 SQL 查询语句（只输出 SQL，不要其他内容）"""


# SQL 错误修正提示
SQL_ERROR_FIX_PROMPT = """你是一个 SQL 调试专家。之前的查询执行失败，请根据错误信息修正 SQL。

原始问题：{query}
执行的 SQL：{sql}
错误信息：{error}
数据库表结构：
{table_schema}

请分析错误原因并输出修正后的 SQL 语句（只输出 SQL，不要其他内容）。"""


# ===== 文档检索 Agent =====
# BM25 + 向量检索的查询扩展
DOC_QUERY_EXPAND_PROMPT = """你是一个查询扩展专家。根据用户原始查询，生成多个扩展查询以提高召回率。

原始查询：{query}

请生成 3 个扩展查询（每个查询一行）：
1. 使用同义词或近义词替换
2. 扩展相关概念
3. 从不同角度描述同一问题"""


# ===== 知识图谱 Agent =====
# 实体链接提示
KG_ENTITY_LINK_PROMPT = """你是一个实体识别专家。从用户问题中提取实体名称。

用户问题：{query}

请识别以下类型的实体：
- 电影名称（中文）
- 演员名称（需同时提供中文名和英文原名，英文原名需与数据库中的格式一致，如 "James Cameron" 而非 "james cameron"）
- 导演名称（需同时提供中文名和英文原名，格式同上）
- 类型名称（如：科幻、动作、爱情等）

实体类型说明：
- actor: 演员，英文名需保持原始大小写格式（如 "Leonardo DiCaprio"）
- director: 导演，英文名需保持原始大小写格式（如 "Steven Spielberg"）
- movie: 电影，使用中文片名
- genre: 类型，使用中文

已知演员/导演的英文名格式参考（供匹配参考）：
{entity_list}

输出格式（JSON）：
{{
    "entities": [
        {{"name": "实体中文名", "name_en": "英文原名（actor/director类型必填，其他类型可为空字符串）", "type": "实体类型"}}
    ]
}}

注意：
1. 如果无法确定英文原名，name_en 填写空字符串
2. 请尽量匹配已知实体列表中的英文名格式"""


# ===== 协商融合 Agent =====
# 三元组抽取提示
TRIPLE_EXTRACTION_PROMPT = """你是一个信息抽取专家。从给定文本中抽取事实三元组（主体, 属性, 值）。

文本内容：
{text}

请抽取所有事实三元组，输出格式（JSON）：
{{
    "triples": [
        {{"subject": "主体", "predicate": "属性", "object": "值"}}
    ]
}}

注意：
1. 数值型事实优先抽取（如评分、票房、年份）
2. 文本描述型事实也要抽取（如类型、导演）
3. 保持三元组的语义完整性"""


# 数值冲突检测提示
CONFLICT_DETECTION_PROMPT = """你是一个冲突检测专家。检测两组三元组之间是否存在数值冲突。

第一组三元组（来源：{source1}）：
{triples1}

第二组三元组（来源：{source2}）：
{triples2}

冲突检测规则：
1. 主体语义相同（同一实体）
2. 属性语义相同（同一属性）
3. 数值偏差 > 5% 或文本描述矛盾，判定为冲突

请输出冲突检测结果（JSON）：
{{
    "conflicts": [
        {{
            "subject": "主体",
            "predicate": "属性",
            "value1": "值1",
            "source1": "来源1",
            "value2": "值2",
            "source2": "来源2",
            "conflict_type": "numeric|textual"
        }}
    ]
}}"""


# 置信度加权融合提示
FUSION_WEIGHTED_PROMPT = """你是一个信息融合专家。根据置信度加权规则，融合多源检索结果。

数据源基础权重：
- sql: 0.5
- doc: 0.3
- kg: 0.2

融合规则：
1. 最终置信度 = 数据源基础权重 × 单条结果置信度
2. 冲突时取置信度最高的结果
3. 差值小于 0.1 则保留多种说法

检索结果：
{results}

请输出融合后的上下文文本，附带来源标签。"""


# ===== 答案生成 Agent =====
# 基于融合上下文生成答案
ANSWER_GENERATION_PROMPT = """你是一个问答专家。基于提供的上下文信息，生成准确的答案。

上下文信息：
{context}

用户问题：{query}

要求：
1. 严格基于上下文信息回答，不要编造内容
2. 每处事实陈述必须标注对应来源编号，格式：[来源X]
3. 如果上下文信息不足以回答问题，明确说明
4. 答案要通顺、准确、完整

请生成答案："""


# 答案来源标注提示
SOURCE_ANNOTATION_PROMPT = """请为以下答案中的每处事实陈述标注来源编号。

答案文本：
{answer}

可用来源：
{sources}

请输出标注来源后的答案，格式：[来源X] 标注在对应事实陈述后。"""


# ===== 通用提示 =====
# 文本摘要提示
TEXT_SUMMARY_PROMPT = """请对以下文本进行摘要，提取关键信息：

{text}

要求：
1. 保留核心事实信息
2. 突出数值型数据
3. 保持语义完整性"""


# 实体消歧提示
ENTITY_DISAMBIGUATION_PROMPT = """你是一个实体消歧专家。判断以下两个实体是否指向同一对象。

实体1：{entity1}
实体2：{entity2}

上下文信息：
{context}

请判断是否为同一实体，并输出：
{{
    "is_same": true/false,
    "confidence": 0.0-1.0,
    "reason": "判断理由"
}}"""
